// Page registry - maps all pages, routes, and dependencies
// This will be populated in Chapter 10 with complete page information

export const pageRegistry = {
  // Each page will be documented here with:
  // - path: route path
  // - component: component location
  // - layout: which layout it uses
  // - dependencies: what common resources it depends on
  // - data: what data files it uses
};
