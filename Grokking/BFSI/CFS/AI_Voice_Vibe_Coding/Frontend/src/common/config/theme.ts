// Common theme configuration
// This file overrides page-specific themes when populated
// Currently empty - pages will use their own local theme styles

export const theme = {
  // Common theme properties will go here when defined
  // These will override page-specific theme settings
};
